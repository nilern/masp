program = expr*

expr = <ws> (atom | compound) <ws>

<compound> = list

list = <"("> expr* <")">

<atom> = number / boolean / ignore / symbol

number = #"\d+"

boolean = "#t" | "#f"

ignore = "#_"

symbol = #"[!?+\-\*/%@#\w]+"

<ws> = #"(\s|;[^\n]*\n)*"
